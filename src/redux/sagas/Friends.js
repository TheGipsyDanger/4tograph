import { call, put } from 'redux-saga/effects'
import { Actions as FriendsActions }from '../reducers/Friends'
import { navigate } from '../../utils/navigation'

export function* init() {
try {
} catch (err) {}
}